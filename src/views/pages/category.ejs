<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../partials/head') %>
</head>

<body>

  <%- include('../partials/header') %>

  <div class="container mt-2">
    <div class="row">
      <div class="col-12">
        <!--the second class, sports in this example, should change depending on the URL we are generating
        RM note 4/1/23: classes are sports, concert, theater, and other-category, can change if that helps class assignment-->
        <div class="jumbotron <%= category %>">
          <h1 class="text-center"><%= category.charAt(0).toUpperCase() + category.substring(1) %></h1>
          <div class="row justify-content-center">


          </div>
          <p class="text-center">Can we put category text here? Might need to add a category table to pull images and
            text</p>
        </div>
      </div>
    </div>
  </div>

  <div id="content">
    <section class="mainContent">
      <% events.forEach(function(event) { %>
        <div class="row" style="margin-top: 25px">
          <div class="col-lg-9 col-12" style="margin-top: 10px">
            <h3 style="text-align: left">
              <a href="/event/<%= event.id %>"><%= event.name %></a>
            </h3>
            <p>
              <%= event.desc %>
            </p>
          </div>
          <div class="col-lg-3 col-12" style="margin-top: 10px">
            <img class="img-fluid" alt="<%= event.name %>" src="<%= event.imgSrc %>" style="max-height: 500px">
          </div>
        </div>
        <% }); %>
      <!--This is an option if we want multiple pages-->
      <nav aria-label="Page navigation">
        <ul class="pagination">
          <li>
            <a href="#" aria-label="Previous">
              <span aria-hidden="true">&laquo;</span>
            </a>
          </li>
          <li><a href="#">1</a></li>
          <li><a href="#">2</a></li>
          <li><a href="#">3</a></li>
          <li><a href="#">4</a></li>
          <li><a href="#">5</a></li>
          <li>
            <a href="#" aria-label="Next">
              <span aria-hidden="true">&raquo;</span>
            </a>
          </li>
        </ul>
      </nav>
    </section>
  </div>
  <footer class="text-center">
    <%- include('../partials/footer') %>
  </footer>
  <%- include('../partials/scripts') %>
</body>

</html>
