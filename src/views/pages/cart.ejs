<!doctype html>

<head>
  <%- include('../partials/head') %>
</head>

<body>
  <header>
    <%- include('../partials/header') %>
  </header>

  <div class="container px-3 my-5 clearfix">
    <!-- Shopping cart table -->
    <div class="card">
      <div class="card-header">
        <h2>Shopping Cart</h2>
      </div>
      <div class="card-body">
        <div class="table-responsive">
          <table class="table table-bordered m-0">
            <thead>
              <tr>
                <!-- Set columns width -->
                <th class="text-center py-3 px-4" style="min-width: 400px;">Event &amp; Details</th>
                <th class="text-right py-3 px-4" style="width: 100px;">Price (Temp Price)</th>
                <th class="text-center py-3 px-4" style="width: 120px;">Number of Tickets</th>
                <th class="text-right py-3 px-4" style="width: 100px;">Total</th>
                <th class="text-center align-middle py-3 px-0" style="width: 40px;"><a href="#"
                    class="shop-tooltip float-none text-light" title="" data-original-title="Clear cart"><i
                      class="ino ion-md-trash"></i></a></th>
              </tr>
            </thead>
            <tbody>
              <% var totalCost = 0; %>
              <% if (items==null) items=[]; %>
                <% items.forEach(function(item) { %>
                  <% totalCost += Math.round((parseFloat(item.price) * parseFloat(item.quantity)) * 100) / 100; %>
                  <tr>
                    <td class="p-4">
                      <div class="media align-items-center">
                        <!-- <img src="https://bootdey.com/img/Content/avatar/avatar1.png" class="d-block ui-w-40 ui-bordered mr-4" alt=""> -->
                        <div class="media-body">
                          <a href="#" class="d-block text-dark"><%= item.event_name %></a>
                          <small>
                            <!--<span class="text-muted">Color:</span> -->
                            <span class="ui-product-color ui-product-color-sm align-text-bottom"
                              style="background:#e81e2c;"></span> &nbsp;
                          </small>
                        </div>
                        <div>
                          <img class="img-fluid" alt="<%= item.event_name %>" src="<%= item.image_url %>" style="max-height: 500px">
                          <div>
                            <p><%= item.event_description %></p>
                          </div>
                        </div>
                      </div>
                    </td>
                    <td class="text-right font-weight-semibold align-middle p-4">$<%= item.price %></td>
                    <td class="text-center align-middle p-4"><%= item.quantity %></td>
                    <td class="text-right font-weight-semibold align-middle p-4">$<%= Math.round((parseFloat(item.price) * parseFloat(item.quantity)) * 100) / 100 %></td>
                    <td class="text-center align-middle px-0"><a href="#"
                        class="shop-tooltip close float-none text-danger" title="" data-original-title="Remove">×</a>
                    </td>
                  </tr>
                <% }); %>
            </tbody>
          </table>
        </div>
        <!-- / Shopping cart table -->

        <div class="d-flex flex-wrap justify-content-between align-items-center pb-4">
          <div class="mt-4">
            <label class="text-muted font-weight-normal">Promocode</label>
            <input type="text" placeholder="ABC" class="form-control">
          </div>
          <div class="d-flex">
            <div class="text-right mt-4 mr-5">
              <label class="text-muted font-weight-normal m-0">Discount</label>
              <div class="text-large"><strong></strong></div>
            </div>
            <div class="text-right mt-4">
              <label class="text-muted font-weight-normal m-0">Total price</label>
              <div class="text-large"><strong>$<%= totalCost %></strong></div>
            </div>
          </div>
        </div>

        <div class="float-right">
          <button type="button" class="btn btn-lg btn-default md-btn-flat mt-2 mr-3">Add another Event</button>
          <button type="button" class="btn btn-lg btn-primary mt-2">Checkout</button>
        </div>
      </div>
    </div>
  </div>
  <%- include('../partials/scripts') %>
</body>